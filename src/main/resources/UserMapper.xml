<mapper namespace="com.test.dao.IUserDao">

    <select id="findAll" resultType="com.test.entity.User" paramterType="com.test.entity.User">
        select * from user where id = #{id} and name = #{name}
    </select>

    <select id="findAll2" resultType="com.test.entity.User" paramterType="com.test.entity.User">
        select * from user
    </select>

</mapper>